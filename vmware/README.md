# Some vmware playbooks

Ansible for Vmware relies on the [pyVmomi](https://github.com/vmware/pyvmomi),
so you'll need to install it before using the playbooks (possibly in a venv
env).
```
$ pip install -r requirements.txt
```


